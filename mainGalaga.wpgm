import wollok.game.*
import player.*
import enemigos.*
import niveles.*

program GalagaGame {
	game.title("Galaga")
	game.height(50) // alto del tablero
	game.width(50) // ancho del tablero
	game.cellSize(50) // aca se setea el tamaña de las celdas "CREO"
	keyboard.space().onPressDo({game.stop()})
	nivelUno.iniciar()
	game.start()
}

